<% if current_user.follower_users.any? %>
  <h3>follow (<%= @current_user.follower_relationships.count %>)</h3>
  <ol class="follower">
<% @users.each do |user|%>
  <h2><%= gravatar_for user %>
  <%= user.name %></h2><br /><hr>
<% end%>
  </ol>
<% else %>
  <ol>友達がいません</ol>
<% end %>